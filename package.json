{
  "name": "soccer-challenge",
  "version": "1.0.0",
  "description": "Jane coding challenge",
  "main": "index.ts",
  "bin": {
    "top-teams": "./bin/top-teams.js"
  },
  "scripts": {
    "init": "npm i && npm run build && npm link",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kyrsten.kelly@gmail.com",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.3.2"
  }
}
